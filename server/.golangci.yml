version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unused

  settings:
    staticcheck:
      checks:
        - all
        - "-SA1029"

  exclusions:
    rules:
      - path: '_test\.go'
        linters:
          - unused
